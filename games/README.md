# /games/

One folder per game, each self-contained and browser-playable:

```
games/
  your-game-name/
    index.html      ← the game (inline JS/CSS preferred)
    (optional small assets)
```

No build steps, no external network calls. See the main README for how to submit.
